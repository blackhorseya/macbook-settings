# tool aliases
alias cat=bat
alias nano=vim
alias tf=terraform

# quick open rc
alias vimrc='${=EDITOR} ~/.vimrc'
alias myclirc='${=EDITOR} ~/.myclirc'

alias lb='docker run --rm \
    -v "$PWD:$PWD" \
    -w="$PWD" \
    lobster-cli:latest'
